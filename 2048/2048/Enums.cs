using System;

namespace _2048
{
    class Enums
    {
        public enum Direction
        {
            Up,
            Down,
            Right,
            Left
        }
        public enum GameStatuses
        {
            Win,
            Lose,
            Idle
        }
    }
}
